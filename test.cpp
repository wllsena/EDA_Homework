#include <iostream>
#include <vector>
#include "trie.cpp"

using namespace std;

/*
int main() {
  trie *tree = initialize_trie();
  put_word(tree, "123", 1);
  put_word(tree, "abd", 2);
  put_word(tree, "abe", 3);

  string word;
  while (true) {
    cin >> word;
    search(tree, word);
  };
};
*/

int main() {
  int fuck[2];
  fuck[0] = 1;
  cout << fuck[0];
  cout << fuck[20];
  cout << fuck[0] << endl;

  return 0;
};
